#include <iostream>
#include <vector>
using namespace std;

int main(){
    int t;
    for(int i = 0; i < t; i ++){
        int n;
        cin >> n;
        vector<int> cities(n, 0);
        for(int i = 0; i < n; i++){
            cin >> cities[i];
        }

        // for(int j = 0)
    }
}